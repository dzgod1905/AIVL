[project]
name = "automation-server"
version = "0.1.0"
description = "Dummy automation-tool catalog + invoke server (contract mục 4)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
]

[tool.uv]
package = false
